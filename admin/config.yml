backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "games"
    label: "Games"
    folder: "_posts/games"
    create: true

    fields:
      - { label: "Title", name: "title", widget: "string" }
